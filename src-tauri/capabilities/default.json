{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Envarsa main window. The webview can only reach the app's own commands; every OS interaction (file dialogs, clipboard, shell reveal) happens in the Rust core.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
